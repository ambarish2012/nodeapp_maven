language: java

jdk:
  - oraclejdk8

build:
  ci:
    # Configure nexus repository settings
    - mkdir ~/.m2
    - cp settings.xml ~/.m2/.
    # Build
    - mvn install
    # Copy artifcats to nexus
    - mvn deploy
